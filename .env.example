# Server
PORT=3307
CORS_ORIGIN=http://localhost:3000

# MySQL (complete these values in .env; do not commit .env)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=cinephoria
DB_CONNECTION_LIMIT=10

# Authentication (use a long random value in .env)
JWT_SECRET=
JWT_EXPIRES_IN=1h
